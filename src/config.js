const host = 'http://barryli.ink:9999'
export const api = {
    loginApi: host + '/librarian/v1/login', // 登入登出
    bookApi: host + '/book/v1/book', // 搜索书籍
    bookTypeApi: host + '/book/v1/bookType',
    wishApi: host + '/book/v1'
}
